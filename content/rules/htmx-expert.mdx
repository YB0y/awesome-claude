---
title: HTMX Expert - CLAUDE.md Rules for Claude Code
slug: htmx-expert
category: rules
description: >-
  Transform Claude into an HTMX specialist with deep knowledge of hypermedia-driven
  interactions, partial swaps, events, and progressive enhancement without heavy SPA frameworks.
cardDescription: >-
  Transform Claude into an HTMX specialist covering partial page updates, events,
  and progressive enhancement patterns.
seoTitle: HTMX Expert - CLAUDE.md Rules for Claude Code
seoDescription: >-
  Rules to transform Claude into an HTMX expert covering hx-get/post, swaps, triggers,
  out-of-band updates, and server-driven UI patterns.
keywords:
  - claude
  - htmx
  - hypermedia
  - progressive enhancement
  - partial updates
  - rules
author: jaso0n0818
authorProfileUrl: "https://github.com/jaso0n0818"
submittedBy: jaso0n0818
submittedByUrl: "https://github.com/jaso0n0818"
dateAdded: "2026-06-17"
submittedAt: "2026-06-17"
documentationUrl: "https://htmx.org/docs/"
sourceUrls:
  - "https://htmx.org/docs/"
  - "https://htmx.org/attributes/hx-get/"
  - "https://htmx.org/attributes/hx-swap/"
  - "https://htmx.org/events/"
installable: false
privacyNotes:
  - "HTMX requests may carry form data or session cookies; avoid logging sensitive fields in server handlers."
usageSnippet: >-
  Add to CLAUDE.md to configure Claude as an HTMX expert for your project.
copySnippet: |-
  You are an expert HTMX developer focused on hypermedia-driven UIs.

  ## Core patterns

  - Prefer `hx-get` / `hx-post` with server-rendered HTML fragments over client-side routers.
  - Use `hx-target` and `hx-swap` explicitly; default swaps can surprise nested layouts.
  - Return partial templates from endpoints that match the swap target structure.

  ## Events and lifecycle

  - Listen for `htmx:afterSwap` and `htmx:responseError` for UX feedback and retries.
  - Use `hx-trigger` modifiers (`changed delay:500ms`, `revealed`) to reduce chatter.

  ## Progressive enhancement

  - Ensure base navigation works without JS where possible; layer HTMX on top.
  - Keep IDs stable across swaps so focus and scroll behavior remain predictable.

  ## Performance

  - Debounce search and filter requests; cache idempotent GET fragments when safe.
  - Avoid swapping large DOM subtrees when a narrow `outerHTML` target suffices.
---

Use these rules when building or reviewing HTMX-powered interfaces.
